# 围棋AI Web前端

基于Vue3的围棋AI网页界面，参考Sabaki实现。

## 功能特性

- 🎮 完整的19×19围棋棋盘界面
- 🤖 支持策略网络和MCTS两种AI模式
- 👥 支持手动对弈、人机对弈、AI自动对弈
- 🎨 现代化的UI设计
- 📱 响应式布局

## 项目结构

```
web-frontend/
├── src/
│   ├── components/       # Vue组件
│   │   ├── Goban.vue     # 围棋棋盘组件
│   │   └── ControlPanel.vue  # 控制面板
│   ├── stores/           # Pinia状态管理
│   │   └── game.js       # 游戏状态
│   ├── services/         # 服务层
│   │   └── gtpClient.js  # GTP协议客户端
│   ├── App.vue           # 主应用组件
│   ├── main.js           # 入口文件
│   └── style.css         # 全局样式
├── server/               # 后端桥接服务
│   ├── gtp_bridge.py     # GTP协议桥接服务器
│   └── requirements.txt  # Python依赖
├── index.html
├── package.json
└── vite.config.js
```

## 安装和运行

### 1. 安装前端依赖

```bash
cd web-frontend
npm install
```

### 2. 安装后端依赖

```bash
cd server
pip install -r requirements.txt
```

### 3. 启动后端服务

```bash
python server/gtp_bridge.py
```

后端服务将在 `http://localhost:8000` 启动。

### 4. 启动前端开发服务器

```bash
npm run dev
```

前端将在 `http://localhost:3000` 启动。

## 使用说明

1. **连接AI**: 点击"连接AI"按钮初始化GTP会话
2. **选择模式**: 
   - 策略网络：快速响应，适合实时对弈
   - MCTS搜索：更强的棋力，但计算时间较长
3. **游戏模式**:
   - 手动模式：可以自由落子，需要手动点击"AI走子"
   - 人机对弈：自动交替，AI会在你落子后自动回应
   - AI自动对弈：两个AI自动对弈
4. **落子**: 直接点击棋盘上的交叉点即可落子

## 技术栈

- **前端**: Vue 3 + Pinia + Vite
- **后端**: Python Flask + Flask-CORS
- **通信**: HTTP REST API
- **协议**: GTP (Go Text Protocol)

## 开发

### 前端开发

```bash
npm run dev
```

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

## 注意事项

1. 确保已训练好所有模型文件（policyNet.pt, playoutNet.pt, valueNet.pt）
2. 确保Python GTP服务可以正常启动
3. 后端服务需要能够访问项目根目录的 `gtp.py` 文件
4. 如果遇到端口冲突，可以修改 `vite.config.js` 和 `gtp_bridge.py` 中的端口配置

## 参考

- [Sabaki](https://sabaki.yichuanshen.de/) - 参考的桌面围棋客户端
- [GTP协议规范](https://www.lysator.liu.se/~gunnar/gtp/)


